import * as React from "react";
import TextField from "@mui/material/TextField";
import { alpha, styled } from "@mui/material/styles";

export default function TextBox(props) {
  const { fontCol, hoverCol, restCol } = props;
  return <h1></h1>;
}
